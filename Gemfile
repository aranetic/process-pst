source :gemcutter

# These gems are only needed for our integration tests, which use
# Ruby to control the process-pst executable and test its output.
gem 'rspec', '1.3.0'
gem 'assert2', '0.5.5'
gem 'nokogiri', '>= 1.4.2' # for assert2
